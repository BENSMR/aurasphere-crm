# Supabase Configuration for AuraSphere CRM
# Copy this file to .env and fill in your credentials
# NEVER commit .env to version control

# ============================================================================
# Supabase Project Details
# ============================================================================

# Your Supabase project URL (public)
SUPABASE_URL=https://lxufgzembtogmsvwhdvq.supabase.co

# Supabase anonymous/public key (safe to expose in browser)
# Used by the Flutter app and client-side tests
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imx4dWZnemVtYnRvZ21zdndoZHZxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg1NTAxMTAsImV4cCI6MjA4NDEyNjExMH0.5Ha4bS4HeuMEqqalpPa_wCSol116rxlllR5s__kVtFs

# Supabase service role key (KEEP SECRET - server-only)
# Only use this on backend servers, NEVER in Flutter or browser code
# You can find this in Supabase Dashboard → Settings → API → Service Role Secret
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================================================
# Server Configuration (for server.js testing)
# ============================================================================

# Server port (default: 5174)
PORT=5174

# Node environment
NODE_ENV=development

# ============================================================================
# Email Configuration (if using custom email provider)
# ============================================================================

# RESEND_API_KEY (if using Resend for email notifications)
# RESEND_API_KEY=re_xxxxx

# TWILIO_ACCOUNT_SID (if using Twilio for WhatsApp)
# TWILIO_ACCOUNT_SID=AC_xxxxx

# TWILIO_AUTH_TOKEN (if using Twilio for WhatsApp)
# TWILIO_AUTH_TOKEN=xxxxx

# ============================================================================
# AI & Integrations
# ============================================================================

# GROQ_API_KEY (for Groq LLM, if not using Edge Functions)
# GROQ_API_KEY=gsk_xxxxx

# STRIPE_SECRET_KEY (if using Stripe)
# STRIPE_SECRET_KEY=sk_test_xxxxx

# PADDLE_API_KEY (if using Paddle)
# PADDLE_API_KEY=xxxxx

# ============================================================================
# CRITICAL NOTES
# ============================================================================

# 1. NEVER expose SUPABASE_SERVICE_ROLE_KEY in browser code or Flutter
#    Only use it on trusted backend servers
#
# 2. SUPABASE_ANON_KEY is public - it's safe to expose in Flutter/browser
#    But ensure CORS and Auth settings are configured in Supabase dashboard
#
# 3. API keys in this file should match what's in your Supabase project:
#    Supabase Dashboard → Settings → API
#
# 4. For Edge Functions, store secrets in Supabase:
#    supabase secrets set MY_SECRET_NAME=value
#
# 5. Test signup with signup-test.html:
#    - Replace YOUR_EMAIL and YOUR_PASSWORD in the HTML
#    - Open in browser (no server needed for client-side test)
#    - Check browser console for errors
#
# 6. Check Supabase auth settings:
#    - Authentication → Providers → Email (must be enabled)
#    - Authentication → Settings → CORS (add your domain)
#    - Authentication → URL Configuration (set Site URL and Redirect URLs)
